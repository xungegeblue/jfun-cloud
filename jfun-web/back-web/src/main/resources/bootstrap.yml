spring:
  profiles:
    active: dev
  application:
    name: BACK-WEB
  cloud:
    nacos:
      config:
        server-addr: ${jfun.nacos-addr}
        file-extension: yml
        prefix: back-web
      discovery:
        server-addr: ${jfun.nacos-addr}

jfun:
  nacos-addr: 127.0.0.1:8848


